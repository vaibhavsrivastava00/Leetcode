class Solution {
public:
    vector<int> sortedSquares(vector<int>& nums) {
        int n = nums.size();
        int s = 0;
        int e = nums.size()-1;
        for(int i = 0;i<n;i++){
            nums[i] *= nums[i]; 
            swap(nums[])
        }
        
    return nums;
    }
};